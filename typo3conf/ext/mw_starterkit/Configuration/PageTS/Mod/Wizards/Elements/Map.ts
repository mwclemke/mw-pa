mod.wizards.newContentElement.wizardItems.mittwald {
    elements {
        mw_starterkit_map {
            iconIdentifier = ce-icon-map
            title = Google Maps
            description = Google Maps
            tt_content_defValues {
                CType = mw_starterkit_map
            }
        }
    }

    show := addToList(mw_starterkit_map)
}